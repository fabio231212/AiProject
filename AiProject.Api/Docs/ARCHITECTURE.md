# 🎨 Arquitectura Visual del Proyecto

## 📊 Diagrama de Capas

```
┌─────────────────────────────────────────────────────────────┐
│                         CLIENTE                              │
│                   (Browser, Postman, App)                    │
└────────────────────────┬────────────────────────────────────┘
                         │ HTTP Request
                         │ POST /api/ai/process
                         ▼
┌─────────────────────────────────────────────────────────────┐
│                    ASP.NET CORE PIPELINE                     │
│  ┌────────────┐  ┌────────────┐  ┌────────────────────┐    │
│  │   HTTPS    │→ │    CORS    │→ │   Authentication   │    │
│  │ Redirect   │  │            │  │    (Future)        │    │
│  └────────────┘  └────────────┘  └────────────────────┘    │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│              FEATURES / VERTICAL SLICES                      │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐   │
│  │         Feature: AiProcessing                        │   │
│  │                                                       │   │
│  │  ┌──────────────────────────────────────────────┐   │   │
│  │  │  AiProcessingEndpoints.cs                    │   │   │
│  │  │  • MapPost("/api/ai/process")                │   │   │
│  │  │  • MapGet("/api/ai/health")                  │   │   │
│  │  │  • Manejo de errores                         │   │   │
│  │  │  • Validación de input                       │   │   │
│  │  └────────────────┬─────────────────────────────┘   │   │
│  │                   │                                  │   │
│  │                   ▼                                  │   │
│  │  ┌──────────────────────────────────────────────┐   │   │
│  │  │  AiProcessingService.cs                      │   │   │
│  │  │  • ProcessAsync()                            │   │   │
│  │  │  • ValidatePromptAsync()                     │   │   │
│  │  │  • Lógica de negocio                         │   │   │
│  │  │  • Llamadas a APIs externas (future)         │   │   │
│  │  └────────────────┬─────────────────────────────┘   │   │
│  │                   │                                  │   │
│  │                   ▼                                  │   │
│  │  ┌──────────────────────────────────────────────┐   │   │
│  │  │  DTOs (Request/Response)                     │   │   │
│  │  │  • AiProcessingRequest.cs                    │   │   │
│  │  │  • AiProcessingResponse.cs                   │   │   │
│  │  └──────────────────────────────────────────────┘   │   │
│  └──────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│               SERVICIOS TRANSVERSALES                        │
│  ┌────────────┐  ┌────────────┐  ┌──────────────────┐      │
│  │  Logging   │  │  Config    │  │  Dependency      │      │
│  │ (ILogger)  │  │ (IConfig)  │  │  Injection       │      │
│  └────────────┘  └────────────┘  └──────────────────┘      │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔄 Flujo de Datos Detallado

```
   CLIENT                    API                    SERVICE
     │                        │                        │
     │  POST /api/ai/process  │                        │
     │───────────────────────>│                        │
     │                        │                        │
     │                        │  1. Validar request    │
     │                        │────────────────────>   │
     │                        │                        │
     │                        │                        │  2. Procesar
     │                        │                        │     lógica
     │                        │                        │───┐
     │                        │                        │   │
     │                        │                        │<──┘
     │                        │                        │
     │                        │  3. Retornar response  │
     │                        │<────────────────────   │
     │                        │                        │
     │  200 OK + JSON         │                        │
     │<───────────────────────│                        │
     │                        │                        │
```

---

## 📦 Estructura de Directorios Explicada

```
AiContext.Api/
│
├── Features/                           ← 🎯 CAPA DE FEATURES
│   └── AiProcessing/                   ← Una feature completa
│       ├── AiProcessingEndpoints.cs    ← 🌐 Capa de Presentación (HTTP)
│       ├── AiProcessingService.cs      ← 💼 Capa de Negocio (Lógica)
│       ├── AiProcessingRequest.cs      ← 📥 DTO de entrada
│       └── AiProcessingResponse.cs     ← 📤 DTO de salida
│
├── Program.cs                          ← 🚀 Entry Point (Bootstrap)
├── appsettings.json                    ← ⚙️  Configuración
├── appsettings.Development.json        ← ⚙️  Config de desarrollo
├── AiContext.Api.http                  ← 🧪 Pruebas HTTP
├── README.md                           ← 📖 Documentación
│
├── bin/                                ← ⚠️  Archivos compilados (ignorar)
└── obj/                                ← ⚠️  Archivos temporales (ignorar)
```

---

## 🔌 Dependency Injection Container

```
┌─────────────────────────────────────────────────────────────┐
│            DEPENDENCY INJECTION CONTAINER                    │
│                     (DI Container)                           │
│                                                              │
│  ┌────────────────────────────────────────────────────┐     │
│  │  Servicios Registrados                             │     │
│  │                                                     │     │
│  │  • ILogger<T>         → ConsoleLogger (Singleton)  │     │
│  │  • IConfiguration     → ConfigurationManager       │     │
│  │  • AiProcessingService → AiProcessingService       │     │
│  │                          (Singleton)               │     │
│  └────────────────────────────────────────────────────┘     │
│                                                              │
│         ▲                           │                        │
│         │                           │                        │
│    Register Services           Resolve Services             │
│    (Program.cs)               (Automático en endpoints)     │
└─────────────────────────────────────────────────────────────┘
```

---

## 🎭 Lifetimes de Servicios Visualizados

### Singleton (Una instancia para toda la app)
```
App Start ──────────────────────────────────────> App End
    │                                                  │
    └──────────────── [Instancia] ───────────────────┘
                    (Vive todo el tiempo)
```

### Scoped (Una instancia por HTTP request)
```
Request 1: ─────> [Instancia A] ─────> ×
Request 2: ─────> [Instancia B] ─────> ×
Request 3: ─────> [Instancia C] ─────> ×
           (Nueva instancia en cada request)
```

### Transient (Nueva instancia cada vez que se solicita)
```
Uso 1 ─────> [Instancia A] ─────> ×
Uso 2 ─────> [Instancia B] ─────> ×
Uso 3 ─────> [Instancia C] ─────> ×
       (Nueva instancia en cada uso)
```

---

## 🌍 Request/Response Flow con Detalles

```
1. HTTP REQUEST
   ├── Headers
   │   ├── Content-Type: application/json
   │   ├── Accept: application/json
   │   └── Authorization: Bearer xxx (future)
   │
   └── Body
       {
         "prompt": "Tu texto aquí"
       }

                    ↓

2. MIDDLEWARE PIPELINE
   ├── HTTPS Redirection
   ├── CORS Check
   ├── Authentication (future)
   ├── Authorization (future)
   └── Routing

                    ↓

3. ENDPOINT HANDLER
   ├── Deserializar JSON → AiProcessingRequest
   ├── Validar datos
   ├── Inyectar dependencias
   └── Llamar al servicio

                    ↓

4. SERVICE LAYER
   ├── Ejecutar lógica de negocio
   ├── Validaciones adicionales
   ├── Llamadas a APIs externas (future)
   └── Generar respuesta

                    ↓

5. HTTP RESPONSE
   ├── Status Code: 200 OK
   ├── Headers
   │   └── Content-Type: application/json
   │
   └── Body
       {
         "result": "Procesado exitosamente...",
         "processedAt": "2024-01-15T10:30:00Z",
         "success": true
       }
```

---

## 🔐 Seguridad (Capas a implementar en el futuro)

```
┌─────────────────────────────────────────────────────────────┐
│  1. HTTPS/TLS                                                │
│     └── Encriptación de datos en tránsito                   │
├─────────────────────────────────────────────────────────────┤
│  2. CORS                                     ✅ Implementado │
│     └── Control de orígenes permitidos                      │
├─────────────────────────────────────────────────────────────┤
│  3. Authentication (JWT)                    🔜 Por implementar│
│     └── Verificar identidad del usuario                     │
├─────────────────────────────────────────────────────────────┤
│  4. Authorization                           🔜 Por implementar│
│     └── Verificar permisos del usuario                      │
├─────────────────────────────────────────────────────────────┤
│  5. Rate Limiting                           🔜 Por implementar│
│     └── Limitar requests por IP/usuario                     │
├─────────────────────────────────────────────────────────────┤
│  6. Input Validation                         ✅ Implementado │
│     └── Validar y sanitizar inputs                          │
├─────────────────────────────────────────────────────────────┤
│  7. Error Handling                           ✅ Implementado │
│     └── No exponer información sensible                     │
└─────────────────────────────────────────────────────────────┘
```

---

## 📈 Escalabilidad Futura

```
FASE 1 (Actual): API Simple
    └── Features/AiProcessing

FASE 2: Agregar más features
    ├── Features/AiProcessing
    ├── Features/UserManagement
    └── Features/Analytics

FASE 3: Agregar persistencia
    ├── Features/...
    └── Infrastructure/
        ├── Database/
        └── Repositories/

FASE 4: Microservicios (si se requiere escala masiva)
    ├── AiProcessing.Service (puerto 5001)
    ├── UserManagement.Service (puerto 5002)
    ├── Analytics.Service (puerto 5003)
    └── API Gateway (puerto 5000)
        └── Enruta requests a servicios
```

---

## 🎓 Conceptos Clave Visualizados

### Record vs Class
```
// Class (mutable)
var request = new AiRequest();
request.Prompt = "Hola";      // ✅ Puedes cambiar
request.Prompt = "Adiós";     // ✅ Puedes cambiar otra vez

// Record (immutable)
var request = new AiProcessingRequest { Prompt = "Hola" };
request.Prompt = "Adiós";     // ❌ ERROR: init-only property
```

### Async/Await
```
// ❌ BLOQUEANTE (Malo)
public string ProcessSync(string prompt)
{
    Thread.Sleep(5000);  // Bloquea el thread
    return "Result";
}

// ✅ NO BLOQUEANTE (Bueno)
public async Task<string> ProcessAsync(string prompt)
{
    await Task.Delay(5000);  // Libera el thread
    return "Result";
}
```

---

Esta arquitectura está diseñada para ser:
- ✅ Fácil de entender
- ✅ Fácil de testear
- ✅ Fácil de mantener
- ✅ Fácil de escalar
- ✅ Fácil de extender con nuevas features
